export type SalonUiModel = {
    id: number;
    name: string;
}

export type SalonServiceUiModel = {
    id: number;
    name: string;
    descriptions: string;
    category: string;
    price: string;
    duration_sec: number;
}